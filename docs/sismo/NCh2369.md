# NCh2369:2025 — Espectro Sísmico de Diseño

Genera y visualiza el espectro de respuesta de diseño según la norma chilena **NCh2369:2025** para estructuras industriales.

---

## Fundamentos

### Forma espectral

La aceleración espectral de diseño $S_a(T)$ se obtiene en tres pasos:

**1. Espectro bruto** (sin reducción):

$$S_a^{\text{bruta}}(T) = A_r \cdot S \cdot \frac{1 + r \cdot \left(\dfrac{T'}{T_0}\right)^p}{1 + \left(\dfrac{T'}{T_0}\right)^q}$$

donde $T' = \alpha_T \cdot T$, con $\alpha_T = 1.0$ horizontal y $\alpha_T = 1.7$ vertical. Para la componente vertical se aplica además un factor $0.7$ sobre el espectro bruto y no se aplica $R^*$.

**2. Factor de amortiguamiento** $\eta$:

$$\eta = \left(\frac{0.05}{\xi}\right)^{0.4}$$

**3. Espectro de diseño reducido**:

$$S_a(T) = I \cdot \eta \cdot \frac{S_a^{\text{bruta}}(T)}{R^*(T)}$$

Para $T < 0.16 \cdot R \cdot T_1$, el factor $R^*$ interpola linealmente desde $1.5$ hasta $R$. Para $T \geq 0.16 \cdot R \cdot T_1$, $R^* = R$.

---

## Parámetros de sitio

### Aceleración efectiva por zona

| Zona | $A_r$ [g] |
|:----:|:---------:|
| 1    | 0.28      |
| 2    | 0.42      |
| 3    | 0.56      |

### Parámetros por tipo de suelo

| Suelo | $S$  | $r$  | $T_0$ [s] | $p$  | $q$  | $T_1$ [s] |
|:-----:|:----:|:----:|:---------:|:----:|:----:|:---------:|
| A     | 0.90 | 4.50 | 0.15      | 1.85 | 3.00 | 0.15      |
| B     | 1.00 | 4.50 | 0.30      | 1.60 | 3.00 | 0.27      |
| C     | 1.05 | 4.50 | 0.40      | 1.50 | 3.00 | 0.35      |
| D     | 1.00 | 3.50 | 0.60      | 1.00 | 2.50 | 0.41      |
| E     | 1.00 | 3.00 | 1.20      | 1.00 | 2.70 | 0.79      |

---

## Calculadora interactiva

<style>
.spec-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(190px, 1fr));
  gap: 0.75rem;
  margin: 1rem 0 0.75rem;
}
.spec-group {
  border: 1px solid rgba(0,0,0,0.14);
  border-radius: 6px;
  padding: 0.8rem 1rem;
}
[data-md-color-scheme="slate"] .spec-group {
  border-color: rgba(255,255,255,0.14);
}
.spec-group-title {
  font-size: 0.72rem;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.08em;
  opacity: 0.5;
  margin-bottom: 0.65rem;
}
.spec-field {
  display: flex;
  flex-direction: column;
  margin-bottom: 0.5rem;
}
.spec-field:last-child { margin-bottom: 0; }
.spec-field label {
  font-size: 0.82rem;
  margin-bottom: 0.2rem;
  opacity: 0.8;
}
.spec-field input,
.spec-field select {
  padding: 0.35rem 0.5rem;
  border: 1px solid rgba(0,0,0,0.2);
  border-radius: 4px;
  font-size: 0.88rem;
  width: 100%;
  box-sizing: border-box;
  background: #fff;
  color: #333;
}
[data-md-color-scheme="slate"] .spec-field input,
[data-md-color-scheme="slate"] .spec-field select {
  background: rgba(255,255,255,0.07);
  color: #eee;
  border-color: rgba(255,255,255,0.2);
}
.spec-btn {
  display: inline-block;
  background: var(--md-primary-fg-color, #3F51B5);
  color: #fff;
  border: none;
  padding: 0.55rem 1.8rem;
  border-radius: 4px;
  font-size: 0.95rem;
  font-weight: 600;
  cursor: pointer;
  letter-spacing: 0.02em;
  margin-bottom: 0.5rem;
}
.spec-btn:hover { opacity: 0.85; }
#spectrum-plot {
  width: 100%;
  height: 450px;
  margin-top: 0.5rem;
}
.spec-table-wrap {
  margin-top: 0.5rem;
  overflow-x: auto;
}
.spec-table-wrap table {
  width: 100%;
  border-collapse: collapse;
  font-size: 0.84rem;
}
.spec-table-wrap th {
  background: var(--md-primary-fg-color, #3F51B5);
  color: #fff;
  padding: 0.4rem 0.9rem;
  text-align: center;
  white-space: nowrap;
}
.spec-table-wrap td {
  padding: 0.28rem 0.9rem;
  text-align: center;
  border-bottom: 1px solid rgba(0,0,0,0.07);
}
[data-md-color-scheme="slate"] .spec-table-wrap td {
  border-bottom-color: rgba(255,255,255,0.07);
}
.spec-table-wrap tr:nth-child(even) td { background: rgba(0,0,0,0.03); }
[data-md-color-scheme="slate"] .spec-table-wrap tr:nth-child(even) td {
  background: rgba(255,255,255,0.04);
}
</style>

<div markdown="0">

<div class="spec-grid">

  <div class="spec-group">
    <div class="spec-group-title">Parámetros del sitio</div>
    <div class="spec-field">
      <label for="zona">Zona sísmica</label>
      <select id="zona">
        <option value="1">Zona 1 — Ar = 0.28 g</option>
        <option value="2" selected>Zona 2 — Ar = 0.42 g</option>
        <option value="3">Zona 3 — Ar = 0.56 g</option>
      </select>
    </div>
    <div class="spec-field">
      <label for="suelo">Tipo de suelo</label>
      <select id="suelo">
        <option value="A">A — Roca sana</option>
        <option value="B">B — Roca blanda / suelo rígido</option>
        <option value="C" selected>C — Suelo denso / firme</option>
        <option value="D">D — Suelo medianamente rígido</option>
        <option value="E">E — Suelo blando / estratificado</option>
      </select>
    </div>
    <div class="spec-field">
      <label for="specI">Factor de importancia I</label>
      <input id="specI" type="number" value="1.2" min="1.0" step="0.1">
    </div>
  </div>

  <div class="spec-group">
    <div class="spec-group-title">Dirección horizontal X</div>
    <div class="spec-field">
      <label for="Rx">Factor de reducción R</label>
      <input id="Rx" type="number" value="3.0" min="1.0" step="0.5">
    </div>
    <div class="spec-field">
      <label for="xix">Amortiguamiento ξ (%)</label>
      <input id="xix" type="number" value="3" min="0.5" max="20" step="0.5">
    </div>
  </div>

  <div class="spec-group">
    <div class="spec-group-title">Dirección horizontal Y</div>
    <div class="spec-field">
      <label for="Ry">Factor de reducción R</label>
      <input id="Ry" type="number" value="3.0" min="1.0" step="0.5">
    </div>
    <div class="spec-field">
      <label for="xiy">Amortiguamiento ξ (%)</label>
      <input id="xiy" type="number" value="3" min="0.5" max="20" step="0.5">
    </div>
  </div>

  <div class="spec-group">
    <div class="spec-group-title">Dirección vertical</div>
    <div class="spec-field">
      <label for="Rv">Factor de reducción R</label>
      <input id="Rv" type="number" value="2.0" min="1.0" step="0.5">
    </div>
    <div class="spec-field">
      <label for="xiv">Amortiguamiento ξ (%)</label>
      <input id="xiv" type="number" value="3" min="0.5" max="20" step="0.5">
    </div>
  </div>

</div>

<button class="spec-btn" onclick="calcSpectrum()">&#9654; Calcular espectro</button>

<div id="spectrum-plot"></div>

<details style="margin-top:1rem">
  <summary style="cursor:pointer; font-weight:600; padding:0.4rem 0; user-select:none">
    Ver tabla de valores
  </summary>
  <div id="spectrum-table" class="spec-table-wrap"></div>
</details>

</div>

<script>
(function () {
  const AR_BY_ZONE = { 1: 0.28, 2: 0.42, 3: 0.56 };
  const SOIL_PARAMS = {
    "A": { S: 0.90, r: 4.50, T0: 0.15, p: 1.85, q: 3.00, T1: 0.15 },
    "B": { S: 1.00, r: 4.50, T0: 0.30, p: 1.60, q: 3.00, T1: 0.27 },
    "C": { S: 1.05, r: 4.50, T0: 0.40, p: 1.50, q: 3.00, T1: 0.35 },
    "D": { S: 1.00, r: 3.50, T0: 0.60, p: 1.00, q: 2.50, T1: 0.41 },
    "E": { S: 1.00, r: 3.00, T0: 1.20, p: 1.00, q: 2.70, T1: 0.79 },
  };

  function specShape(ar, sp, T, scaleFactor, periodShift) {
    if (T === 0) return scaleFactor * ar * sp.S;
    const ratio = (periodShift * T) / sp.T0;
    return scaleFactor * ar * sp.S *
      (1 + sp.r * Math.pow(ratio, sp.p)) /
      (1 + Math.pow(ratio, sp.q));
  }

  function rStar(T, R, t1) {
    const limit = 0.16 * R * t1;
    if (limit <= 0 || T >= limit) return R;
    return 1.5 + (R - 1.5) * (T / limit);
  }

  function computeSpectrum(zone, soil, I, R, damp, scaleFactor, periodShift, applyRStar) {
    const ar = AR_BY_ZONE[zone];
    const sp = SOIL_PARAMS[soil];
    const eta = Math.pow(0.05 / damp, 0.4);
    const periods = [], accels = [];
    for (let i = 0; i <= 500; i++) {
      const T = Math.round(i * 0.01 * 1e4) / 1e4;
      const sa = specShape(ar, sp, T, scaleFactor, periodShift);
      const rEff = applyRStar ? rStar(T, R, sp.T1) : R;
      periods.push(T);
      accels.push(I * eta * sa / rEff);
    }
    return { periods, accels };
  }

  function interp(accels, T) {
    return accels[Math.min(Math.round(T / 0.01), accels.length - 1)];
  }

  window.calcSpectrum = function () {
    if (typeof Plotly === 'undefined') {
      alert('Plotly aún está cargando, intenta en un momento.');
      return;
    }
    const zone  = parseInt(document.getElementById('zona').value);
    const soil  = document.getElementById('suelo').value;
    const I     = parseFloat(document.getElementById('specI').value);
    const Rx    = parseFloat(document.getElementById('Rx').value);
    const xix   = parseFloat(document.getElementById('xix').value) / 100;
    const Ry    = parseFloat(document.getElementById('Ry').value);
    const xiy   = parseFloat(document.getElementById('xiy').value) / 100;
    const Rv    = parseFloat(document.getElementById('Rv').value);
    const xiv   = parseFloat(document.getElementById('xiv').value) / 100;

    const sp    = SOIL_PARAMS[soil];
    const specX = computeSpectrum(zone, soil, I, Rx, xix, 1.0, 1.0, true);
    const specV = computeSpectrum(zone, soil, I, Rv, xiv, 0.7, 1.7, false);

    const isDark = document.body.getAttribute('data-md-color-scheme') === 'slate';
    const tmpl   = isDark ? 'plotly_dark' : 'plotly_white';

    const traces = [{
      x: specX.periods, y: specX.accels, mode: 'lines',
      name: 'Horizontal X  (R=' + Rx + ', ξ=' + (xix * 100).toFixed(1) + '%)',
      line: { color: '#1f77b4', width: 2.5 }
    }];

    let specY = specX;
    if (Ry !== Rx || xiy !== xix) {
      specY = computeSpectrum(zone, soil, I, Ry, xiy, 1.0, 1.0, true);
      traces.push({
        x: specY.periods, y: specY.accels, mode: 'lines',
        name: 'Horizontal Y  (R=' + Ry + ', ξ=' + (xiy * 100).toFixed(1) + '%)',
        line: { color: '#ff7f0e', width: 2.5, dash: 'dash' }
      });
    }

    traces.push({
      x: specV.periods, y: specV.accels, mode: 'lines',
      name: 'Vertical  (R=' + Rv + ', ξ=' + (xiv * 100).toFixed(1) + '%)',
      line: { color: '#2ca02c', width: 2, dash: 'dot' }
    });

    const saMax = Math.max.apply(null, specX.accels);
    traces.push({
      x: [sp.T0, sp.T0], y: [0, saMax * 1.08], mode: 'lines',
      name: 'T₀ = ' + sp.T0 + ' s (Suelo ' + soil + ')',
      line: { color: 'grey', width: 1, dash: 'dashdot' }
    });

    Plotly.react('spectrum-plot', traces, {
      template: tmpl,
      title: {
        text: 'Espectro NCh2369:2025 — Zona ' + zone + ', Suelo ' + soil + ', I = ' + I,
        font: { size: 14 }
      },
      xaxis: { title: 'Período T [s]', range: [0, 5], dtick: 0.5, minor: { dtick: 0.1 } },
      yaxis: { title: 'Sa [g]', rangemode: 'tozero' },
      legend: { x: 0.65, y: 0.98, bgcolor: 'rgba(0,0,0,0)' },
      margin: { t: 50, b: 60, l: 65, r: 20 },
      hovermode: 'x unified'
    }, { responsive: true });

    // ── Tabla de valores ─────────────────────────────────────────────────────
    const T_TABLA = [0, 0.05, 0.10, 0.15, 0.20, 0.30, 0.40, 0.50,
                     0.75, 1.00, 1.25, 1.50, 2.00, 2.50, 3.00, 4.00, 5.00];
    const showY = (Ry !== Rx || xiy !== xix);
    let html = '<table><thead><tr><th>T [s]</th><th>Sa X [g]</th>';
    if (showY) html += '<th>Sa Y [g]</th>';
    html += '<th>Sa V [g]</th></tr></thead><tbody>';
    for (const T of T_TABLA) {
      html += '<tr><td>' + T.toFixed(2) + '</td>';
      html += '<td>' + interp(specX.accels, T).toFixed(4) + '</td>';
      if (showY) html += '<td>' + interp(specY.accels, T).toFixed(4) + '</td>';
      html += '<td>' + interp(specV.accels, T).toFixed(4) + '</td></tr>';
    }
    html += '</tbody></table>';
    document.getElementById('spectrum-table').innerHTML = html;
  };
})();
</script>
